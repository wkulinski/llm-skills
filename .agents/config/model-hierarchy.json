{
    "_comment": "Copy this file to <project-root>/.agents/config/model-hierarchy.json, then replace every example profile with an exact model and reasoning pair available in that project.",
    "_matching_comment": "Model identity is compared without the provider prefix: `commandcode/deepseek/model-x` and `deepseek/model-x` describe the same profile, while the reasoning level must match exactly.",
    "_ordering_comment": "Keep profiles ordered from the strongest pair to the weakest acceptable pair, like a leaderboard. The executor compares array indexes and treats a current profile as sufficient when its index is less than or equal to the required profile index.",
    "_reasoning_comment": "Each model and reasoning combination is a separate profile. Use identifiers exposed by the runtime; the provider prefix may be omitted because matching ignores it, but the reasoning level must be observable and exact.",
    "version": 1,
    "order": "strongest-to-weakest",
    "profiles": [
        {
            "model": "openai/gpt-6-sol",
            "reasoning": "medium"
        },
        {
            "model": "openai/gpt-6-luna",
            "reasoning": "max"
        },
        {
            "model": "commandcode/deepseek/deepseek-v4.1-flash",
            "reasoning": "max"
        },
        {
            "model": "openai/gpt-6-luna",
            "reasoning": "xhigh"
        }
    ]
}
