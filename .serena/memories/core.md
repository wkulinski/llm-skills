Curated reusable LLM skills catalog. Source files: `.agents/skills/<skill>/SKILL.md`; shared helpers/references under `.agents/skills/_shared/`; Vitest regression tests mirror helpers under `tests/skills/`. Keep changes scoped to one skill. `var/` and local env are ignored. Read `mem:tech_stack`, `mem:conventions`, `mem:suggested_commands`, and `mem:task_completion` for operational details.